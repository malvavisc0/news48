[project]
name = "news48"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dateparser",
    "feedparser",
    "html-to-markdown",
    "httpx",
    "llama-index-llms-openai-like",
    "loguru",
    "pydantic",
    "python-dotenv",
    "rich",
    "typer",
]

[project.scripts]
news48 = "main:main"

[tool.setuptools]
packages = ["agents", "commands", "database"]
py-modules = ["config", "helpers", "models", "main"]

[tool.uv]
package = true

[dependency-groups]
dev = ["black", "isort", "pytest"]
