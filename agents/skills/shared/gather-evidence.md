# Skill: gather-evidence

## Trigger
Always active — all agents must ground reasoning in observable state.

## Rules
1. Run CLI commands to gather evidence before making decisions.
2. Never assume system state — always verify with `--json` commands.
3. Record what you observed, not what you expect.
