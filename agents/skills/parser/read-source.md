# Skill: Read the source before parsing

## Trigger
Always active — parser must read HTML before extracting.

## Rules
1. Always read the HTML file before extracting data.
2. Use `read_file` tool on the provided HTML path.
